# =============================================================================
# PK-HELP Custom - environment variables
#
# Local development (recommended):
#   copy .env.example .env.local
#   fill values below, then: npm run db:push && npm run seed:components
#
# Production (Vercel / Render / Railway):
#   set the same keys in the host dashboard (never commit .env / .env.local)
# =============================================================================

# --- Database (required) ---
# Neon: Dashboard -> Connection string -> pooled URL (append ?sslmode=require if missing)
DATABASE_URL="postgresql://neondb_owner:npg_VPHY52tenTSM@ep-tiny-frog-aq67dvjg-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# --- Site URL (required in production) ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Domains (production) ---
NEXT_PUBLIC_MAIN_DOMAIN="pk-help.pl"
NEXT_PUBLIC_POLISH_DOMAIN="pk-help-pl.pl"

# NEXT_PUBLIC_FORCE_LOCALE=pl

# --- Admin auth (required in production) ---
JWT_SECRET="f7Kx9LmQ2vNp8RtYc4ZdHs1WaE6BjUo3"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin"

# --- Telegram (recommended for order notifications) ---
# Create bot via @BotFather, add bot to group/channel, get chat id via getUpdates
TELEGRAM_BOT_TOKEN="8682816608:AAEt2iRpI7_Cg223S0NMrnbIw7rG6rbx4MA"
TELEGRAM_CHAT_ID="7949715597"

# --- Public links (optional) ---
NEXT_PUBLIC_TELEGRAM_URL="https://t.me/blank"
NEXT_PUBLIC_INSTAGRAM_URL="https://instagram.com/pkhelp"
NEXT_PUBLIC_GOOGLE_MAPS_EMBED_URL="https://maps.app.goo.gl/qGo52S7FUxVbyGie8"

# --- Vercel Blob (required for admin uploads in production) ---
# BLOB_READ_WRITE_TOKEN=""

# RATE_LIMIT_MAX=5
# RATE_LIMIT_WINDOW_MS=60000
